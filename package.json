{
  "name": "flying-solo-fashion-week-contract-builder",
  "private": true,
  "type": "module",
  "scripts": {
    "build:vendor": "esbuild src/vendor/toastui-browser-entry.mjs --bundle --minify --format=iife --target=safari15 --outfile=public/vendor/toastui-editor-all.min.js",
    "start": "node server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@toast-ui/editor": "^3.2.2",
    "dompurify": "^3.4.13",
    "marked": "^18.0.9"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
